#### 项目框架：

1. 登录
   └─ 选择头像 + 输入昵称 → 注册用户信息
2. 创建或加入房间
   ├─ 创建房间：设置人数上限(2-8)、每轮时间(60/90/120s)、总轮数(3-10)
   └─ 加入房间：输入6位房间号
3. 等待大厅 (room.status = 'waiting')
   ├─ 非房主：点击"准备"
   └─ 房主：等所有人准备后，点击"开始游戏"
4. 游戏进行 (room.status = 'playing')
   每轮流程：
   ├─ 出题阶段 (round.status = 'choosing')
   │   └─ 画手在弹窗中输入本轮词条
   ├─ 画画阶段 (round.status = 'drawing')
   │   ├─ 画手在画布上作画，笔画实时同步
   │   ├─ 猜手输入猜测，猜对 +10 分
   │   └─ 画手每有一人猜对 +5 分
   │   └─ 倒计时结束 → 房主端触发 endRound
   └─ 回合结束
   ├─ 未到总轮数 → 轮换画手，开始新一轮
   └─ 已到总轮数 → 游戏结束
5. 游戏结束 (room.status = 'finished')
   └─ 弹窗显示最终排名和分数

#### 目前需解决的BUG

* [X] 每一轮游戏结束后画板要自动清除；
* [ ] 画笔同步有延迟：滑动屏幕后线条会消失将近一秒再出现；
* [X] 所有人都猜对后应该直接结束本轮游戏开始下一轮；
* [X] 画板底部有部分是失效的，无法清除，也无法同步；
* [ ] 玩家头像不同步的问题；
* [X] 画手与猜手那画板大小的问题；
* [ ] 游戏轮数应该是玩家数的倍数；
* [ ] 每轮结束之后应该公布上一轮的词是什么；

UI上应该改进的地方：

* [X] 顶部白色背景去掉，字体颜色改为白色；
* [X] Rank页面太简陋了；

#### 后续应增加的功能

1. [ ] 用户无法修改头像、昵称等；
2. [ ] 等待阶段玩家无法取消准备；
3. [ ] 邀请好友功能；
